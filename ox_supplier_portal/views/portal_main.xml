<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="sale.portal_my_home_sale" name="Show Quotations / Sales Orders" customize_show="True" inherit_id="portal.portal_my_home" priority="20">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="inside">
            <!-- <t t-call="portal.portal_docs_entry">
                <t t-set="title">Quotations</t>
                <t t-set="url" t-value="'/my/quotes'"/>
                <t t-set="placeholder_count" t-value="'quotation_count'"/>
            </t>
            <t t-call="portal.portal_docs_entry">
                <t t-set="title">Sales Orders</t>
                <t t-set="url" t-value="'/my/orders'"/>
                <t t-set="placeholder_count" t-value="'order_count'"/>
            </t> -->
        </xpath>
    </template>

    <template id="purchase.portal_my_home_menu_purchase" name="Portal layout : purchase menu entries" inherit_id="portal.portal_breadcrumbs" priority="25">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'rfq' or order and order.state == 'sent'" t-attf-class="breadcrumb-item #{'active ' if not order else ''}">
                <a t-if="order" t-attf-href="/my/rfq?{{ keep_query() }}">Requests for Quotation</a>
                <t t-else="">Requests for Quotation</t>
            </li>
            <li t-if="page_name == 'purchase' or order and order.state != 'sent'" t-attf-class="breadcrumb-item #{'active ' if not order else ''}">
                <a t-if="order" t-attf-href="/my/purchase?{{ keep_query() }}">Purchase Orders</a>
                <t t-else="">Purchase Orders</t>
            </li>
            <li t-if="order" class="breadcrumb-item active">
                <t t-esc="order.name"/>
            </li>
        </xpath>
    </template>

    <template id="portal.portal_layout" name="Portal Layout">
        <t t-call="portal.frontend_layout">
            <t t-set="is_portal" t-value="True"/>

            <div t-if="not no_breadcrumbs and not my_details and not breadcrumbs_searchbar" class="o_portal container mt-3">
                <div class="row align-items-center bg-white g-0 border rounded">
                    <div class="col-10">
                        <t t-call="portal.portal_breadcrumbs"></t>
                    </div>
                    <div t-if="prev_record or next_record" class="col-2 flex-grow-0 text-center">
                        <t t-call='portal.record_pager'/>
                    </div>
                </div>
            </div>
            <div id="wrap" class='o_portal_wrap'>
                <div class="container mb64">
                    <t t-if="my_details">
                        <div class="row justify-content-between mt-4">
                            <div t-attf-class="col-12 col-md col-lg-6">
                                <t t-out="0"/>
                            </div>
                            <div id="o_my_sidebar" class="pt-3 pt-lg-0 col-12 col-md col-lg-4 col-xl-3 o_my_sidebar">
                                <div class="o_my_contact" t-if="sales_user">
                                    <t t-call="portal.portal_contact"/>
                                </div>
                                <div style="margin-top:50px;">
                                    <form t-if="user_id.partner_id._get_status_supplier() == 'none' and page_home=='supplier'" action="/my/request_supplier_register" method="post">
                                        <p>
                                            <h4>¿Interesado en convertirse en proveedor?</h4>
                                        </p>
                                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                        <button type="submit" class="btn btn-danger">Solicitar Registro Proveedor</button>
                                    </form>
                                    <div t-if="user_id.partner_id._get_status_supplier() != 'none' and page_home=='supplier'" style="background-color:#FFF;display:block;min-height:200px;padding:20px;">
                                        <span style="font-weight:bold;">Status: </span>
                                        <span t-esc="user_id.partner_id.supplier_status" style="font-size:18px;color:#0081A1;" />
                                        <br />
                                        <p style="font-size:12px;" t-if="user_id.partner_id._get_status_supplier() == 'requires_specifications'">
                                            Nota: Ingrese al formulario de registro, entontrará las observaciones relacionadas por las cuales se paso la solicitud de registro a este estado.
                                        </p>
                                        <hr />
                                        <div class="progress">
                                            <div class="progress-bar bg-danger" t-att-class="'bg-success' if user_id.partner_id._get_link_up().progress_status == 100 else 'bg-danger'" role="progressbar" t-attf-style="{{user_id.partner_id._get_link_up()._get_progress_portal()}}" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>
                                        </div>

                                        <span t-esc="str(user_id.partner_id._get_link_up().progress_status) + '%'"/>
                                        <br />
                                        <a t-if="user_id.partner_id._get_status_supplier() in ('pre_registered', 'requires_specifications')" t-attf-href="/my/supplier/register">Completar Registro Proveedor</a>
                                        <br />
                                        <br />
                                        <p style="font-size:12px;">Una vez complete el registro, se notificará al área encargada para validar la información ingresada, 
                                        cuando el estado del resgistro sea 'Aceptado', será notificado por correo electrónico y pordrá continuar con el proceso de cotización y/o ejecución.</p>
                                    </div>
                                </div>
                                <br t-if="user_id.partner_id._get_status_supplier() == 'none' and page_home=='supplier'"/><hr t-if="user_id.partner_id._get_status_supplier() == 'none' and page_home=='supplier'"/><br t-if="user_id.partner_id._get_status_supplier() == 'none' and page_home=='supplier'"/>
                                <div class="o_portal_my_details">
                                    <h4>Datos de la cuenta</h4>
                                    <hr class="mt-1 mb-0"/>
                                    <div t-field="user_id.partner_id" t-options='{"widget": "contact", "fields": ["email", "phone", "address", "name"]}'/>
                                    <hr t-if="user_id.partner_id.image_1920"/>
                                    <img t-if="user_id.partner_id.image_1920" t-attf-src="data:image/jpg;base64,{{user_id.partner_id.image_1920}}" style="width:150px;height:150px" />
                                    <hr />
                                    <a href="/my/data"><i class="fa fa-pencil mx-1"/>Editar Mis Datos</a>
                                </div>
                                <div class="o_portal_my_security mt-3">
                                    <h4>Account Security </h4>
                                    <hr class="mt-1 mb-1"/>
                                    <a href="/my/security"><i class="fa fa-pencil mx-1"/>Edit Security Settings</a>
                                </div>
                            </div>
                        </div>
                    </t>
                    <t t-else="">
                        <t t-out="0"/>
                    </t>
                </div>
            </div>
        </t>
    </template>

    <template id="auth_totp_portal.totp_portal_hook" name="TOTP Portal hook" inherit_id="portal.portal_my_security">
        <xpath expr="//section[@name='portal_change_password']" position="after">
        </xpath>
    </template>

    <template id="portal.portal_my_security">
        <t t-call="portal.portal_layout">
            <div class="o_portal_security_body">
                <t t-set="additional_title">Security</t>
                <t t-set="no_breadcrumbs" t-value="1"/>
                <div class="alert alert-danger" role="alert" t-if="get_error(errors)">
                    <t t-esc="errors"/>
                </div>
                <section name="portal_change_password">
                    <h3>Change Password</h3>
                    <t t-set="path">password</t>
                    <div class="alert alert-success" role="alert" t-if="success and success.get('password')">
                    Password Updated!
                    </div>
                    <div class="alert alert-danger" role="alert" t-if="get_error(errors, 'password')">
                        <t t-esc="errors['password']"/>
                    </div>
                    <form action="/my/security" method="post" class="oe_reset_password_form">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <input type="hidden" name="op" value="password"/>
                        <div class="mb-3">
                            <label for="current">Password:</label>
                            <input type="password" t-attf-class="form-control form-control-sm {{ 'is-invalid' if get_error(errors, 'password.old') else '' }}" id="current" name="old" autocomplete="current-password" required="required"/>
                            <div class="invalid-feedback">
                                <t t-esc="get_error(errors, 'password.old')"/>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="new">New Password:</label>
                            <input type="password" t-attf-class="form-control form-control-sm {{ 'is-invalid' if get_error(errors, 'password.new1') else '' }}" id="new" name="new1" autocomplete="new-password" required="required"/>
                            <div class="invalid-feedback">
                                <t t-esc="get_error(errors, 'password.new1')"/>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="new2">Verify New Password:</label>
                            <input type="password" t-attf-class="form-control form-control-sm {{ 'is-invalid' if get_error(errors, 'password.new2') else '' }}" id="new2" name="new2" autocomplete="new-password" required="required"/>
                            <div class="invalid-feedback">
                                <t t-esc="get_error(errors, 'password.new2')"/>
                            </div>
                        </div>
                        <button type="submit" class="btn btn-secondary">Change Password</button>
                        <a style="margin-left: 40px" t-attf-href="/my">Cancelar</a>
                    </form>
                </section>
            </div>
        </t>
    </template>

</odoo>





